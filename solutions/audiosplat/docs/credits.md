# Credits and dependencies

AudioSplat's V1 runtime uses browser APIs and project-owned code and SVG assets.
Development uses TypeScript, Vite, Vitest, jsdom, and Playwright. Review package
licenses and `npm audit --omit=dev` before each release.
