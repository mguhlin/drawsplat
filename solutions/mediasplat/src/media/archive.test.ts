import { describe, expect, it } from "vitest"; import { createZip } from "./archive";
describe("createZip", () => { it("packages every named result in a standard ZIP structure", async () => { const zip = await createZip([{ name: "part-01.mp3", blob: new Blob(["one"]) }, { name: "part-02.mp3", blob: new Blob(["two"]) }]); const buffer = await new Promise<ArrayBuffer>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(reader.result as ArrayBuffer); reader.onerror = reject; reader.readAsArrayBuffer(zip); }); const bytes = new Uint8Array(buffer); const text = new TextDecoder().decode(bytes); expect(zip.type).toBe("application/zip"); expect(text).toContain("part-01.mp3"); expect(text).toContain("part-02.mp3"); expect(new DataView(bytes.buffer).getUint32(bytes.length - 22, true)).toBe(0x06054b50); }); });
